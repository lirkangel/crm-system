# Optional: if Testcontainers cannot find Docker, set a unix socket URL, e.g.:
# docker.host=unix:///Users/you/.docker/run/docker.sock
# (Values here are not shell-expanded; use a literal path or set DOCKER_HOST in the environment.)
